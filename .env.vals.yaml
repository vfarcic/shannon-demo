# vals references (Google Cloud Secret Manager) — NOT secrets themselves.
# Safe to commit: the value stays in GCP and only the repo owner can resolve it.
# devbox resolves this into the shell when USE_VALS=1 (see devbox.json).
# A viewer without access simply gets prompted for their own key by `./dot.nu run`.
SHANNON_AI_API_KEY: ref+gcpsecrets://vfarcic/open-router-key
